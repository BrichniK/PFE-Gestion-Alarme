import { Routes } from '@angular/router';
import { ResetComponent } from './reset.component';

export default [
    {
        path: '',
        component: ResetComponent,
        title: 'Reset',
    },
] as Routes;
