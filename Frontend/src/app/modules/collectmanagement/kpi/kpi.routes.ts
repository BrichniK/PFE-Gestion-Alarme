import { KpiComponent } from './kpi.component';
import { Routes } from '@angular/router';

export default [
    {
        path: '',
        component: KpiComponent,
        title: 'KPI',
    },
] as Routes;
